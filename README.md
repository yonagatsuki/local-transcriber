# Local Transcriber

A local web tool that transcribes audio or video files with the OpenAI API. It also provides sentence-level language explanations in the selected display language.

## Run

Create `.env` from `.env.example` and add your OpenAI API key.

```powershell
.\start-local.ps1
```

Open:

```text
http://localhost:3000
```
